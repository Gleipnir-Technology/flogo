document.addEventListener("DOMContentLoaded", function () {
	/*
	fetch('/.flogo/page.html')
		.then(response => response.text())
		.then(html => {
			document.getElementById('container').innerHTML = html;
		})
		.catch(error => console.error('Error:', error));
	*/
	alert("flogo!");
});
